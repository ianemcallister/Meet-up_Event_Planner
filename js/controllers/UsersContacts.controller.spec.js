describe('UsersContacts.controller', function() {
	it('collect the users contacts from firebase', function() {
		var foo = 0; foo += 1; expect(foo).toEqual(1);
	});
	it('display all the users contacts', function() {
		var foo = 0; foo += 1; expect(foo).toEqual(1);
	});
	it('allow the user to update a contact', function() {
		var foo = 0; foo += 1; expect(foo).toEqual(1);
	});
	it('allow the user to delete a contact', function() {
		var foo = 0; foo += 1; expect(foo).toEqual(1);
	});
});