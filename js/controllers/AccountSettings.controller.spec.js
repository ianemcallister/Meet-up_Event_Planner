describe('AccontSettings.controller', function() {
	
	it('allows the user to set their biographical information for thier account', function() {
		var foo = 0; foo += 1; expect(foo).toEqual(1);
	});
});