describe('SelectedEvents.controller', function() {
	it('loads an event from Firebase', function() {
		var foo = 0; foo += 1; expect(foo).toEqual(1);
	});
	it('allows registered users to edit event details for an event they\'re hosting', function() {
		var foo = 0; foo += 1; expect(foo).toEqual(1);
	});
	it('verifies that all fields are valid', function() {
		var foo = 0; foo += 1; expect(foo).toEqual(1);
	});
	it('allows a list of recipients to be added to the event and contacts', function() {
		var foo = 0; foo += 1; expect(foo).toEqual(1);
	});
	it('allows registered users to RSVP to an event they\'re invited to', function() {
		var foo = 0; foo += 1; expect(foo).toEqual(1);
	});
	it('allows registered user to delete an event they are hosting', function() {
		var foo = 0; foo += 1; expect(foo).toEqual(1);
	});
	it('allows registered users to edit event detils', function() {
		var foo = 0; foo += 1; expect(foo).toEqual(1);
	});
});