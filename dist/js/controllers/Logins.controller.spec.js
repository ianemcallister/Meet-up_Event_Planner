describe('Logins.controller', function() {
	it('allows new users to login as a guest', function() {
		var foo = 0; foo += 1; expect(foo).toEqual(1);
	});
	it('allows new users to create a user account', function() {
		var foo = 0; foo += 1; expect(foo).toEqual(1);
	});
	it('allows registered users to access their profile', function() {
		var foo = 0; foo += 1; expect(foo).toEqual(1);
	});
	it('saves the uid to maintain state throught the app', function() {
		var foo = 0; foo += 1; expect(foo).toEqual(1);
	});
	it('saves the session token to manage access', function() {
		var foo = 0; foo += 1; expect(foo).toEqual(1);
	});
	it('verifies that new users entered a valid e-mail', function() {
		var foo = 0; foo += 1; expect(foo).toEqual(1);
	});
	it('verifies that new users entered a valid password ', function() {
		var foo = 0; foo += 1; expect(foo).toEqual(1);
	});
	it('after login redirects to the user\'s intended destination', function() {
		var foo = 0; foo += 1; expect(foo).toEqual(1);
	});
});