describe('UserEvents.controller', function() {
	it('collect the users events from firebase', function() {
		var foo = 0; foo += 1; expect(foo).toEqual(1);
	});
	it('displays all the events the user is hosting', function() {
		var foo = 0; foo += 1; expect(foo).toEqual(1);
	});
	it('displays all the events the user is RSVPed for', function() {
		var foo = 0; foo += 1; expect(foo).toEqual(1);
	});
	it('displays all the user\'s pending event invitations', function() {
		var foo = 0; foo += 1; expect(foo).toEqual(1);
	});
});