describe('NewVisitorConversions.controller', function() {
	it('allows new users create a registered user account', function() {
		var foo = 0; foo += 1; expect(foo).toEqual(1);
	});
	it('verifies that new users entered a valid e-mail', function() {
		var foo = 0; foo += 1; expect(foo).toEqual(1);
	});
	it('verifies that new users entered a valid password ', function() {
		var foo = 0; foo += 1; expect(foo).toEqual(1);
	});
	it('redirects to the login controller ', function() {
		var foo = 0; foo += 1; expect(foo).toEqual(1);
	});
});